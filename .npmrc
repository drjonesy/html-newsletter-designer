# pnpm is the only supported package manager for this project.
engine-strict=true

# Hoist nothing by default — keeps phantom dependencies from compiling.
# Vite/Tailwind plugins resolve fine under pnpm's strict node_modules layout.
shamefully-hoist=false
strict-peer-dependencies=false

# Reproducible installs in CI.
auto-install-peers=true
